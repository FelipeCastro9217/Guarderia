@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<!-- Views/Clientes/Index.cshtml -->
@model IEnumerable<Guarderia.Models.Cliente>
@{
    ViewData["Title"] = "Gestión de Clientes";
}

<div class="container mt-4">
    <h2>@ViewData["Title"]</h2>
    
    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Mensaje"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <p>
        <a asp-action="Crear" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Nuevo Cliente
        </a>
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Nombre Completo</th>
                    <th>Teléfono</th>
                    <th>Email</th>
                    <th>Dirección</th>
                    <th>Fecha Registro</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdCliente</td>
                        <td>@item.Nombre @item.Apellido</td>
                        <td>@item.Telefono</td>
                        <td>@item.Email</td>
                        <td>@item.Direccion</td>
                        <td>@item.FechaRegistro.ToString("dd/MM/yyyy")</td>
                        <td>
                            <a asp-action="Detalles" asp-route-id="@item.IdCliente" class="btn btn-sm btn-info" title="Ver Detalles">
                                Detalles
                            </a>
                            <a asp-action="Editar" asp-route-id="@item.IdCliente" class="btn btn-sm btn-warning" title="Editar">
                                Editar
                            </a>
                            <a asp-action="Eliminar" asp-route-id="@item.IdCliente" class="btn btn-sm btn-danger" title="Eliminar">
                                Eliminar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>



